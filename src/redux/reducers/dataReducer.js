// import constants from '../constants';

var initialState = {
  countryForumns: {},
  posts: [],
  post: [],
  loading: false,
};

export default (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
